<Page x:Class="WpfExercises.Views.QuestionEditor"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfExercises.Views"
        xmlns:VM="clr-namespace:WpfExercises.ViewModels"
        mc:Ignorable="d"
        Title="QuestionEditor" Height="450" Width="800">
    <Page.DataContext>
        <VM:QuestionEditorViewModel />
    </Page.DataContext>
    <Grid>
        <TextBox 
            Background="Pink"
            Width="360"
            Height="40"
            FontSize="36"
            Text="{Binding Path=CurrentQuestionText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}">
        </TextBox>
    </Grid>
</Page>
